export interface selectInt {
    workingYears: string[],
    Welfares: string[],
    PayMonths: string[],
    PayFilter: string[],
}

export interface selectKeyOptionInt {
    key: string,
    name: string,
    inp: string
}